[project]
name = "inrat-monitor"
version = "1.2.3"
readme = "README.md"
requires-python = ">=3.12"
authors = [{name = "Andrey Morozov", email = "mandrei@mks.ru"}]
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.ruff.lint]
select = ["E", "W", "F", "I"]
